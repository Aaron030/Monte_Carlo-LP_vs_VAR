% !TEX root = ../main.tex

\section{Extension 1: Dynamic Misspecification (3-4 pages)}

\subsection{Simulation Design}

Extension~1 examines 
whether the finite-sample MSE ranking between LP(4) and the complexity-adjusted 
VAR sweep is robust to dynamic misspecification of the Wold representation. The 
baseline VAR(4) is augmented by a first-order moving average component, yielding 
a VARMA(4,1) DGP:

\begin{equation}
    y_t = A_1 y_{t-1} + A_2 y_{t-2} + A_3 y_{t-3} + A_4 y_{t-4} 
    + u_t + \Theta u_{t-1}, \quad u_t = B\varepsilon_t, \quad 
    \varepsilon_t \overset{\text{iid}}{\sim} \mathcal{N}(0, I_2)
\end{equation}

where $\Theta = \theta I_2$ is a scalar-scaled MA impact matrix, with 
misspecification strength $\theta \in \{0.1, 0.3, 0.6\}$. The case $\theta = 0$ 
nests back to the baseline VAR(4), ensuring continuity across designs. The VAR 
coefficient matrices $A_1, \ldots, A_4$ and the impact matrix $B$ are inherited 
from the baseline DGP, so that all variation across Extension~1 scenarios is 
attributable exclusively to the MA component.

The VARMA(4,1) DGP is invertible for all $\theta \in \{0.1, 0.3, 0.6\}$, 
admitting an infinite-order VAR representation. This means the complexity-adjusted 
VAR sweep retains its theoretical motivation: higher-order VAR$(q)$ specifications 
progressively approximate the true dynamics as $q \to \infty$, though at the cost 
of increasing parameter proliferation in finite samples. The central question is 
therefore whether LP(4) --- which is robust to this misspecification by virtue of 
its zero asymptotic bias \parencite{olea2025local} --- retains its MSE 
advantage over the equal-lag VAR(4), and where on the complexity-adjusted VAR 
frontier it sits relative to the baseline.

Persistence is varied across $\rho \in (0.5, 0.95)$ as in the baseline, 
since the degree to which the MA component distorts finite-sample VAR 
approximations depends directly on the persistence of the underlying process: 
at high $\rho$, shocks decay slowly and the MA term remains influential at 
longer horizons, amplifying the VAR's approximation bias. Sample sizes follow 
the baseline design, $T \in \{100, 200, 240, 500\}$. The misspecification 
strength $\theta \in \{0.1, 0.3, 0.6\}$ is thus varied jointly with 
persistence and sample size, yielding a three-way design that isolates the 
marginal contribution of MA misspecification from the persistence and 
finite-sample channels already documented in the baseline.

The structural estimand $\theta_h = \Psi_h^{\mathrm{VARMA}} B e_1$ is defined 
with respect to the true VARMA moving-average matrices $\Psi_h^{\mathrm{VARMA}}$, 
computed analytically from the VARMA companion representation prior to each 
simulation run. Both estimators --- LP(4) and the VAR$(q)$ sweep --- are applied 
without modification to the VARMA-generated data, so that the misspecification 
is fully absorbed into the finite-sample performance of each estimator rather 
than accommodated by the estimation procedure. Performance measures are identical 
to the baseline.

\subsection{Simulation Results}


% ----------------------------------- True IRF
\begin{figure}[htbp]
\centering
\caption{True IRF Functions by MA Misspecification Strength}
\label{fig:DYN_MISSPEC_IRF}

\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_IRF_rho=0.5.png}
    \label{fig:DYN_MISSPEC_IRF_rho05}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_IRF_rho=0.7.png}
    \label{fig:DYN_MISSPEC_IRF_rho07}
\end{subfigure}

\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_IRF_rho=0.95.png}
    \label{fig:DYN_MISSPEC_IRF_rho095}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} True structural IRF \(\theta_h\) by horizon, one line per MA misspecification strength \(\theta \in \{0,0.1,0.3,0.6\}\) (\(\theta=0\) nests the baseline VAR(4)). Panels correspond to persistence \(\rho \in \{0.5,0.7,0.95\}\).
\end{minipage}

\end{figure}


% ----------------------------------- Small Sample Deviation from Truth
\begin{figure}[htbp]
\centering
\caption{Small-Sample Deviation from the True IRF}
\label{fig:DYN_MISSPEC_SMALL_SAMPLE_DEV}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_LIMITED_DEVIATION_rho=0.5_T=240_B=5000.png}
    \label{fig:DYN_MISSPEC_SMALL_SAMPLE_DEV_rho05}
\end{subfigure}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_LIMITED_DEVIATION_rho=0.7_T=240_B=5000.png}
    \label{fig:DYN_MISSPEC_SMALL_SAMPLE_DEV_rho07}
\end{subfigure}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_LIMITED_DEVIATION_rho=0.95_T=240_B=5000.png}
    \label{fig:DYN_MISSPEC_SMALL_SAMPLE_DEV_rho095}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Deviation from truth, \(\hat\theta_h - \theta_h\), at \(T=240\) over \(B=5{,}000\) replications, for VAR(4) (black) and LP(4) (crimson). Solid line is the mean deviation (finite-sample bias); shaded area is the central \(90\%\) Monte Carlo band (empirical 5th--95th percentile), not an SE band. Within each panel, sub-axes correspond to \(\theta \in \{0,0.1,0.3,0.6\}\); panels to persistence \(\rho \in \{0.5,0.7,0.95\}\).
\end{minipage}

\end{figure}


% ----------------------------------- Asymptotic Deviation from Truth
\begin{figure}[htbp]
\centering
\caption{Asymptotic Deviation from the True IRF}
\label{fig:DYN_MISSPEC_ASYMPTOTIC}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_ASYMPTOTIC_DEVIATION_rho=0.5_T=10000_B=1000.png}
    \label{fig:DYN_MISSPEC_ASYMPTOTIC_rho05}
\end{subfigure}


\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_ASYMPTOTIC_DEVIATION_rho=0.7_T=10000_B=1000.png}
    \label{fig:DYN_MISSPEC_ASYMPTOTIC_rho07}
\end{subfigure}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_ASYMPTOTIC_DEVIATION_rho=0.95_T=10000_B=1000.png}
    \label{fig:DYN_MISSPEC_ASYMPTOTIC_rho095}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} As above, at \(T=10{,}000\) over \(B=1{,}000\) replications. LP(4) (crimson) lies on the zero line at all horizons, whereas VAR(4) (black) retains a small, non-vanishing short-horizon bias. Solid line is the mean deviation; shaded area is the central \(90\%\) Monte Carlo band. Sub-axes within each panel correspond to \(\theta \in \{0,0.1,0.3,0.6\}\); panels to \(\rho \in \{0.5,0.7,0.95\}\).
\end{minipage}

\end{figure}

% ----------------------------------- Point Estimates
\begin{figure}[htbp]
\centering
\caption{Point-Estimation Performance Across Persistence Regimes}
\label{fig:DYN_MISSPEC_POINT_ESTIMATION}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_POINT_ESTIMATION_rho=0.5_T=240_B=5000_theta=0.6.png}
    \label{fig:DYN_MISSPEC_POINT_ESTIMATION_rho05}
\end{subfigure}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_POINT_ESTIMATION_rho=0.7_T=240_B=5000_theta=0.6.png}
    \label{fig:DYN_MISSPEC_POINT_ESTIMATION_rho07}
\end{subfigure}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_POINT_ESTIMATION_rho=0.95_T=240_B=5000_theta=0.6.png}
    \label{fig:DYN_MISSPEC_POINT_ESTIMATION_rho095}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each panel reports bias, variance, and RMSE of the estimated impulse responses \(\hat\theta_h\) relative to the true estimand \(\theta_h\), horizon-by-horizon for \(h = 1, \ldots, 20\), computed across \(B = 5{,}000\) Monte Carlo replications with \(T = 240\). The fixed LP(4) reference line is compared against the complexity-adjusted VAR\((q)\) sweep. Panels correspond to persistence regimes \(\rho \in \{0.5, 0.7, 0.95\}\). Misspecification level $\theta$ of 0.6. Shaded area in bias represents 95\% band from Monte Carlo standard errors.
\end{minipage}

\end{figure}

% ----------------------------------- Coverage
\begin{figure}[htbp]
\centering
\caption{Confidence-Interval Coverage under Dynamic Misspecification}
\label{fig:DYN_MISSPEC_COVERAGE}

\begin{subfigure}[t]{0.9\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_COVERAGE_rho=0.5_T240_B=5000.png}
    \label{fig:DYN_MISSPEC_COVERAGE_rho05}
\end{subfigure}

\begin{subfigure}[t]{0.9\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_COVERAGE_rho=0.7_T240_B=5000.png}
    \label{fig:DYN_MISSPEC_COVERAGE_rho07}
\end{subfigure}

\begin{subfigure}[t]{0.9\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_COVERAGE_rho=0.95_T240_B=5000.png}
    \label{fig:DYN_MISSPEC_COVERAGE_rho095}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Empirical coverage of nominal \(95\%\) confidence intervals by horizon for VAR(4) and LP(4) at \(T=240\) over \(B=5{,}000\) Monte Carlo replications, with one curve per MA misspecification strength \(\theta \in \{0,0.1,0.3,0.6\}\). VAR(4) intervals are constructed via the delta method; LP(4) intervals use heteroskedasticity-robust standard errors. The rightmost panel validates that the VAR(4) delta-method standard error continues to track the empirical sampling dispersion at \(\theta=0.6\). Panels correspond to persistence values \(\rho \in \{0.5,0.7,0.95\}\).
\end{minipage}

\end{figure}

% ----------------------------------- Sample RMSE by Horizon X
\begin{figure}[htbp]
\centering
\caption{Sample RMSE by Horizon for Dynamic Misspecification}
\label{fig:DYN_MISSPEC_SAMPLE_RMSE}

\begin{subfigure}[t]{0.5\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_RMSE_BY_HORIZON_rho=0.5_T=240.png}
    \label{fig:DYN_MISSPEC_SAMPLE_RMSE_rho05}
\end{subfigure}

\begin{subfigure}[t]{0.5\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_RMSE_BY_HORIZON_rho=0.7_T=240.png}
    \label{fig:DYN_MISSPEC_SAMPLE_RMSE_rho07}
\end{subfigure}

\begin{subfigure}[t]{0.5\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{DYN_MISSPEC_RMSE_BY_HORIZON_rho=0.95_T=240.png}
    \label{fig:DYN_MISSPEC_SAMPLE_RMSE_rho095}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each panel reports RMSE by horizon for the dynamically misspecified DGP with varying $T$. Sub-graphs correspond to persistence values $\rho \in \{0.5,0.7,0.95\}$.
\end{minipage}

\end{figure}

\FloatBarrier
\subsection{Discussion of Results}

\FloatBarrier