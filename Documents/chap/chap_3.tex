% !TEX root = ../main_ext_1.tex

\section{Baseline Simulation}
Because \textcite{ludwig2026local}'s contribution is analytical and deliberately abstains 
from a simulation-based horse race, whether the conventional finite-sample conclusions 
survive once the equal-lag restriction is relaxed remains open. We therefore operationalize 
his central implication in a controlled, correctly specified setting. Rather than comparing a 
fixed $\text{LP}(4)$ to a single equal-lag $\text{VAR}(4)$, we locate it on the full VAR-order 
frontier from $\text{VAR}(1)$ to $\text{VAR}(p+H-1)$, rendering the implicit complexity of the 
direct projection visible as a position on that frontier. Because the identity in Equation 
\ref{eq:ludwig_identity} maps $\text{LP}(p)$ to a sequence of VARs rather than to any single 
higher-order VAR, no individual VAR is its perfect counterpart, so the $\text{LP}(4)$ is read 
as a reference line against the frontier, against which its bias, variance, and coverage are 
located. Beyond operationalizing the complexity argument, this correctly specified design 
also serves as the reference against which the later extensions measure the effect of 
introducing misspecification.

\subsection{Simulation Design}
The underlying DGP is a stable bivariate zero-mean $\text{VAR}(4)$ with recursive Cholesky 
identification. Consistent with the theoretical setup, this imposes a contemporaneous zero 
restriction where the first variable does not respond on impact to shocks in the second 
variable, while the second variable may respond to both shocks simultaneously.
%
\begin{equation}
    y_t = A_1 y_{t-1} + A_2 y_{t-2} + A_3 y_{t-3} + A_4 y_{t-4} + u_t, 
    \quad y_t \in \mathbb{R}^2
\end{equation}
%
\begin{equation}
    \Sigma_u = BB', \qquad 
    B = \begin{pmatrix} 1 & 0 \\ 0.1 & 0.5 \end{pmatrix}, 
    \quad b_{11}, b_{22} > 0
\end{equation}

% \begin{equation}
%     \rho(\mathcal{A}) < 1, \qquad 
%     \mathcal{A} = \begin{pmatrix} 
%         A_1 & A_2 & A_3 & A_4 \\ 
%         I_2 & \mathbf{0}   & \mathbf{0}   & \mathbf{0}   \\ 
%         \mathbf{0}   & I_2 & \mathbf{0}   & \mathbf{0}   \\ 
%         \mathbf{0}   & \mathbf{0}   & I_2 & \mathbf{0}   
%     \end{pmatrix} \in \mathbb{R}^{8 \times 8}
% \end{equation}
%
Persistence is calibrated via the spectral radius of the companion matrix $\mathcal{A}$ across 
the empirically relevant range $\rho \in \{0.5, 0.7, 0.95\}$. The lower bound reflects weakly 
persistent processes where impulse responses decay rapidly, rendering bias-variance comparisons 
at longer horizons uninformative. The upper bound approaches the unit-root boundary while 
maintaining covariance stationarity, covering the high-persistence region where finite-sample 
differences between estimators are most pronounced. 

The structural target of interest is the impulse response function, which traces the dynamic 
causal effect of a structural innovation on the system over time. We normalize the innovations 
as $u_t = B\varepsilon_t$ with $\varepsilon_t \overset{\text{iid}}{\sim} \mathcal{N}(\mathbf{0}, 
I_2)$, so that a unit innovation in $\varepsilon_{1,t}$ is by construction a one-standard-
deviation shock. The population estimand at horizon $h$ is then
%
\begin{equation}
    \theta_h = \Psi_h B e_1, \qquad e_1 = (1, 0)', \qquad h = 0, 1, \ldots, 20,
\end{equation}
%
where $\Psi_h$ is the reduced-form moving-average matrix generated by the companion 
recursion $\Psi_h = \sum_{j=1}^{\min(h,4)} A_j \Psi_{h-j}$ with $\Psi_0 = I_2$, $B$ is the 
lower-triangular Cholesky factor of $\Sigma_u$, and $e_1$ selects the first column of the 
structural impact matrix, isolating the propagation of the first shock while discarding 
contemporaneous variation from the second. Because the baseline DGP is linear and covariance 
stationary, $\theta_h$ coincides with the population conditional expectation of the system 
given the structural shock and constitutes the invariant benchmark that both the iterated 
$\text{VAR}(q)$ specifications and the direct $\text{LP}(4)$ regressions attempt to recover 
in finite samples.
%
% ----------------------------------- True IRF
\begin{figure}[htbp]
\centering
\caption{True Impulse Response Functions Across Persistence Regimes}
\label{fig:BL_TRUE_IRF}


\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{BASE_IRF_rho=0.5.png}
    \label{fig:BL_TRUE_IRF_LOW}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{BASE_IRF_rho=0.7.png}
    \label{fig:BL_TRUE_IRF_MID}
\end{subfigure}

\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{BASE_IRF_rho=0.95.png}
    \label{fig:BL_TRUE_IRF_HIGH}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each panel reports the true structural impulse response function of 
variables one and two to a one-standard-deviation shock in the first structural innovation, 
traced over horizons 
\(h = 1, \ldots, 20\). Panels correspond to persistence regimes 
\(\rho \in \{0.5, 0.7, 0.95\}\). Blue line represents $y_1 \leftarrow \epsilon_1 
($\textit{estimand} $ \theta_h)$, while grey line shows $y_2 \leftarrow \epsilon_1$.
\end{minipage}

\end{figure}
%

We draw Gaussian innovations throughout, so that the conditional-expectation impulse 
response coincides with the linear projection that both estimators target \parencite{plagborg2021local}. The choice is 
otherwise innocuous, since the performance metrics depend only on the second moments of 
the data, and holding the distribution fixed across all designs ensures that later differences 
reflect the misspecification channel alone.

Sample sizes are set to $T \in \{100, 250, 500\}$, each generated after a 200-period 
burn-in to remove initialization effects. The lower bound $T=100$ matches the median 
sample size documented by \textcite{herbst2024bias} in the applied LP literature and 
also marks a practical floor, since estimating models up to $\text{VAR}(p+H-1)$ at 
smaller $T$ rapidly exhausts degrees of freedom. The intermediate $T=250$ represents 
a comfortably large empirical dataset, roughly twenty years of monthly or sixty years of 
quarterly data, while $T=500$ serves as a large-sample reference for convergence toward the 
population equivalence of \textcite{plagborg2021local}.

We compare two classes of models. The first is a fixed $\text{LP}(4)$ estimator, 
which serves as the reference point. At each horizon $h = 1, \ldots, H$, the 
$\text{LP}(4)$ estimator is obtained from the OLS regression
\begin{equation}
    y_{1,t+h} = \mu_h + \beta_h \hat{\varepsilon}_{1,t} + \sum_{\ell=1}^{4} \delta_{h,\ell}' y_{t-\ell} + \xi_{h,t}, \quad h = 1, \ldots, 20
\end{equation}
where $\hat{\varepsilon}_{1,t} = e_1'(\hat{B}^{(4)})^{-1}\hat{u}_t^{(4)}$ is the estimated structural shock recovered from a first-stage $\text{VAR}(4)$. While the true structural shock is perfectly observable by construction in our data-generating process, directly projecting on this true innovation would artificially advantage the local projection by bypassing identification uncertainty. To maintain a rigorously fair comparison, we intentionally restrict the $\text{LP}(4)$ to use the empirically estimated shock. This guarantees that both estimators face the exact same identification problem. Consequently, any divergence in finite-sample performance is driven strictly by differences in dynamic propagation rather than discrepancies in shock recovery. The structural impulse response estimate at horizon $h$ is then simply the slope coefficient $\hat{\theta}_h^{\mathrm{LP}} = \hat{\beta}_h$.

The second class consists of a sweep of VAR estimators of increasing order, 
$\text{VAR}(q)$ for $q = 1, \ldots, p + H - 1$, where $p = 4$ and $H = 20$. For each order 
$q$, the VAR is estimated equation-by-equation via OLS. The reduced-form covariance matrix 
$\hat{\Sigma}_u^{(q)}$ is Cholesky-decomposed to recover the structural impact matrix 
$\hat{B}^{(q)}$, and the $h$-step ahead structural IRF is computed as
\begin{equation}
    \hat{\theta}_h^{\mathrm{VAR}(q)} = \hat{\Psi}_h^{(q)} \hat{B}^{(q)} e_1
\end{equation}
where $\hat{\Psi}_h^{(q)}$ denotes the $h$-step ahead moving-average matrix of the estimated 
$\text{VAR}(q)$. 

No data-dependent lag selection is performed within the sweep. Every VAR order 
$q = 1, \ldots, p + H - 1$ is estimated on each simulated sample, so the comparison evaluates 
fixed specifications rather than data-driven selection procedures.

Performance is assessed along two dimensions, both reported horizon by horizon for $h = 1, 
\ldots, 20$. For point estimation we evaluate the bias, variance, mean squared error, and root 
mean squared error of the impulse response estimates relative to the true population estimand, 
computed across $B = 5{,}000$ Monte Carlo replications \parencite{li2024local}. Monte Carlo 
standard errors accompany every point-estimation measure to quantify the remaining simulation 
uncertainty.

For inference we assess the empirical coverage of nominal $95\%$ confidence intervals, the 
fraction of replications whose interval brackets the true structural estimand. For the 
$\text{VAR}(q)$ sweep these intervals are built by the delta method, which propagates the 
reduced-form coefficient uncertainty through the nonlinear mapping to the structural responses 
\parencite{kilian2017structural}. For the fixed $\text{LP}(4)$ they use heteroskedasticity-robust 
standard errors, which are asymptotically valid here given the martingale-difference property of 
the structural shock under recursive identification \parencite{plagborg2021local}.



\subsection{Simulation Results}

Figure \ref{fig:BL_COMPLEXITY} traces the horizon-averaged root mean squared error against VAR
order $q$ for the three persistence regimes and sample sizes, with $\text{LP}(4)$ as a horizontal
reference. Two level effects set the stage. The error falls monotonically with the sample size
and rises with persistence, so the whole frontier and its $\text{LP}(4)$ line shift down as $T$
grows and up as $\rho$ grows. At the conventional equal-lag point $q=4$, the VAR sits below its
$\text{LP}(4)$ line in every panel, the familiar reading that low-order VARs are the more
efficient point estimators.

What the frontier adds is where $\text{LP}(4)$ sits once the equal-lag restriction is dropped.
The optimum is a low VAR order, a shallow minimum near $q=2$, and the curve rises beyond it into a
U-shape that sharpens with persistence, from smooth at $\rho=0.5$ to a steep drop and clear U near
the unit-root boundary. At $T=100$ the rising frontier crosses the $\text{LP}(4)$ line only near
$q \approx 15$ at low and intermediate persistence, and at a slightly lower order at $\rho=0.95$,
so the fixed $\text{LP}(4)$ already carries the effective complexity of a heavily parameterized
VAR rather than a parsimonious one.

The sample size sharpens this picture. As $T$ grows the frontier flattens and its minimum drifts
to higher orders, and at $T=500$ the VAR frontier and the $\text{LP}(4)$ reference line draw
together, the gap between them narrowing markedly relative to the smaller samples. This
convergence is the finite-sample counterpart of the population equivalence of
\textcite{plagborg2021local}. As the sample grows the iterated VAR and the direct projection
recover the same linear model, and the finite-sample gap that the complexity frontier exposes
shrinks away.

% ----------------------------------- Complexity Frontier
\begin{figure}[htbp]
\centering
\caption{Complexity Frontier Across Persistence Regimes}
\label{fig:BL_COMPLEXITY}

\begin{subfigure}[t]{0.45\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{BASE_COMPLEXITY_FRONTIER_rho=0.5.png}
    \label{fig:BL_COMPLEXITY_LOW}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.45\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{BASE_COMPLEXITY_FRONTIER_rho=0.7.png}
    \label{fig:BL_COMPLEXITY_MID}
\end{subfigure}

\begin{subfigure}[t]{0.45\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{BASE_COMPLEXITY_FRONTIER_rho=0.95.png}
    \label{fig:BL_COMPLEXITY_HIGH}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Horizon-averaged RMSE of the structural IRF estimate as a function of VAR order 
\(q = 1, \ldots, 23\) (solid lines), with the fixed LP(4) estimator shown as a horizontal reference 
line of the same colour (dashed), for each sample size \(T \in \{100, 250, 500\}\). The vertical
dotted line marks the equal-lag VAR(4); \(B = 5{,}000\) replications. Panels correspond to persistence 
\(\rho \in \{0.5, 0.7, 0.95\}\). Colors blue, red, yellow represent sample sizes 100, 250, 500, respectively.
\end{minipage}

\end{figure}

Figure \ref{fig:BL_PE} reports bias, variance, MSE, and RMSE at $T=250$, horizon by horizon
across the three persistence regimes, with $\text{LP}(4)$ against a representative VAR set. Bias
and variance are ordered by lag length, the classic tradeoff. The low-order VARs are the most
biased but shed it fastest, and higher orders track the $\text{LP}(4)$ bias path progressively
longer, with $\text{VAR}(23)$ following it throughout. In variance the ordering reverses, the
low-order VARs collapsing toward zero earliest while the heavily parameterized $\text{VAR}(16)$
and $\text{VAR}(23)$ overshoot $\text{LP}(4)$ before matching it later. $\text{LP}(4)$ itself
stays the stable middle reference, and root mean squared error follows variance apart from a
short-horizon $\text{VAR}(2)$ spike that carries its bias.

Persistence scales this pattern rather than changing it. The horizon at which the estimators
diverge moves slightly later as $\rho$ rises, and the levels grow. At $\rho=0.7$ the same ordering
holds, with $\text{VAR}(2)$ now overshooting enough to leave the correctly specified
$\text{VAR}(4)$ the most accurate. Near the unit-root boundary at $\rho=0.95$ everything is
elevated, $\text{VAR}(2)$ turns erratic, the remaining VARs and $\text{LP}(4)$ share a common
downward path, and because none regains efficiency at long horizons the root mean squared error
takes a distinctly arched profile.


% ----------------------------------- Point Estimates
\begin{figure}[htbp]
\centering
\caption{Point-Estimation Performance Across Persistence Regimes}
\label{fig:BL_PE}

\begin{subfigure}[t]{0.55\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{BASE_POINT_ESTIMATION_rho=0.5_T=250_B=5000.png}
    \label{fig:BL_LOW_PE}
\end{subfigure}

\begin{subfigure}[t]{0.55\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{BASE_POINT_ESTIMATION_rho=0.7_T=250_B=5000.png}
    \label{fig:BL_MID_PE}
\end{subfigure}

\begin{subfigure}[t]{0.55\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{BASE_POINT_ESTIMATION_rho=0.95_T=250_B=5000.png}
    \label{fig:BL_HIGH_PE}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each panel reports bias, variance, and RMSE of the estimated impulse responses 
\(\hat\theta_h\) relative to the true estimand \(\theta_h\), horizon-by-horizon for \(h = 1, \ldots, 20\), 
computed across \(B = 5{,}000\) Monte Carlo replications with \(T = 250\). The fixed LP(4) reference line is 
compared against the complexity-adjusted VAR\((q)\) sweep. Panels correspond to persistence regimes 
\(\rho \in \{0.5, 0.7, 0.95\}\). Shaded area in bias represents 95\% band from Monte Carlo standard 
errors. Line color represents model architecture, with blue, black, light green, dark green, 
lilac, red, representing VAR(2), VAR(4), VAR(8), VAR(16), VAR(23), and LP(4), respectively.
\end{minipage}

\end{figure}

\clearpage

Figure \ref{fig:BL_COV} reports the empirical coverage of nominal 95 percent confidence intervals
at $T=250$ across the three persistence regimes, alongside the $\text{VAR}(4)$ delta-method
standard error against its empirical sampling standard deviation. The $\text{LP}(4)$ holds near
nominal throughout, while the $\text{VAR}(4)$ delta-method intervals are strongly
persistence-sensitive, over-covering at low persistence and under-covering badly near the unit
root.

Persistence drives the pattern. The $\text{LP}(4)$ stays tight around $0.95$ at low and
intermediate persistence and slips only to about $0.90$ at $\rho=0.95$. The $\text{VAR}(4)$
instead swings with persistence, climbing to full coverage at $\rho=0.5$ and then deteriorating
until it falls below $0.80$ at $\rho=0.95$, the worst of the three. The high-order $\text{VAR}(23)$
tracks the same arc more gently, drifting above nominal at low persistence and settling near the
$\text{LP}(4)$ around $0.90$ at the boundary.

The right column shows the miscalibration is not a broken standard error. At $\rho=0.5$ and
$\rho=0.7$ the $\text{VAR}(4)$ delta-method standard error and the empirical sampling standard
deviation are nearly indistinguishable, so the coverage gaps there are structural. Only at
$\rho=0.95$ does a vertical gap open, with the empirical dispersion sitting above the analytical
standard error at short and intermediate horizons before the two converge by the end of the
window.

% ----------------------------------- Coverage
\begin{figure}[htbp]
\centering
\caption{Confidence-Interval Coverage Across Persistence Regimes}
\label{fig:BL_COV}

\begin{subfigure}[t]{0.7\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{BASE_COVERAGE_rho=0.5_T=250_B=5000.png}
    \label{fig:BL_LOW_CI}
\end{subfigure}

\begin{subfigure}[t]{0.7\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{BASE_COVERAGE_rho=0.7_T=250_B=5000.png}
    \label{fig:BL_MID_CI}
\end{subfigure}

\begin{subfigure}[t]{0.7\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{BASE_COVERAGE_rho=0.95_T=250_B=5000.png}
    \label{fig:BL_HIGH_CI}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each subfigure corresponds to a persistence regime \(\rho \in \{0.5, 0.7, 0.95\}\) 
and contains two panels. The \emph{left panel} reports the empirical coverage of nominal \(95\%\) 
confidence intervals --- the fraction of replications in which the interval contains the true 
estimand \(\theta_h\) --- horizon-by-horizon for \(h = 1, \ldots, 20\), with \(T = 250\) and 
\(B = 5{,}000\). Coverage is shown for VAR(4) (black), VAR(23) (green), and LP(4) (red), with 
the dotted horizontal line marking the nominal \(95\%\) level. VAR\((q)\) intervals are 
constructed via the delta method; LP(4) intervals use heteroskedasticity-robust standard errors. 
The \emph{right panel} validates the VAR(4) delta-method standard error against the empirical Monte 
Carlo sampling standard deviation of \(\hat\theta_h\): the empirical SD is shown in blue and the mean 
delta-method SE in orange, so that close agreement indicates an accurate analytical SE while a gap 
signals that it mis-states the true sampling dispersion.
\end{minipage}

\end{figure}

\clearpage
\FloatBarrier

\subsection{Discussion of Results}

Because an $\text{LP}(p)$ algebraically embeds a sequence of VAR predictions of orders $p$ 
through $p+h-1$, its coherent benchmark is the full VAR-order sweep rather than a single 
equal-order VAR. The point-estimation advantage of the equal-lag $\text{VAR}(4)$ replicates the 
standard consensus \parencite{li2024local, olea2025local}, but locating the local projection on 
the frontier reveals where it actually falls. The $\text{LP}(4)$ does not sit near the 
parsimonious end. It behaves like a highly parameterized system, intersecting the sweep beyond 
$q \approx 15$.

The bias-variance tradeoff is governed by persistence. Under low persistence the frontier is 
monotonic, so the variance penalty for over-parameterization outweighs any bias reduction once 
the impulse response decays quickly. Near the unit-root boundary the pronounced U-shape instead 
exposes the cost of lag truncation, where the downward bias in the autoregressive coefficients 
dominates the error budget \parencite{kilian1998small}. Here the local projection loses its 
robustness advantage. It provides no relief from this persistence-driven bias while carrying the 
higher variance of its horizon-by-horizon regressions, so it is dominated by the equal-lag VAR.

Root mean squared error alone obscures a divergence between point estimation and inference. The 
coverage results show that the VAR's parametric efficiency comes at the cost of inferential 
fragility, as the $\text{VAR}(4)$ delta-method intervals fail for structural reasons across the 
persistence spectrum. At weak persistence they over-cover, because the response converges to 
zero and the interval captures the unconditional mean rather than signalling precision. At high 
persistence the first-order Taylor approximation behind the delta method breaks down and 
understates the dispersion of the skewed finite-sample distribution \parencite{kilian1999finite}, 
producing steep under-coverage. The $\text{LP}(4)$, by contrast, holds near-nominal coverage 
across all regimes, with its heteroskedasticity-robust standard errors remaining valid even as 
the process approaches non-stationarity \parencite{montiel2021local}.


\FloatBarrier