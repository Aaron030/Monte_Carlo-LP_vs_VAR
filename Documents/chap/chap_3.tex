% !TEX root = ../main.tex

\section{Baseline Simulation (4-5 pages)}


This confounding motivates the first part of our study. Because Ludwig's contribution is analytical and deliberately abstains from a simulation-based horse race, the question of whether the conventional finite-sample conclusions survive once the equal-lag restriction is relaxed remains open. We therefore operationalize Ludwig's central implication through a complexity-adjusted benchmarking exercise that holds parsimony more nearly fixed, comparing a fixed LP(4) estimator against progressively higher-order VAR specifications that proxy its increasing effective dynamic flexibility. Crucially, because the identity in Theorem 1 maps LP(p) to a sequence of VARs rather than to any single higher-order VAR, no individual VAR is the appropriate counterpart; instead, LP(4) is read as a reference line against the full VAR order frontier, against which its bias, variance, and coverage can be located. We frame the exercise as a test of robustness rather than as a foregone conclusion: the aim is to assess whether the conventional bias-variance verdict persists, dissolves, or reverses once the comparison is placed on more comparable terms, and to identify the features of the data-generating process under which each procedure is favored.

\subsection{Simulation Design}
\textbf{AIM:} This simulation study examines whether conventional finite-sample 
conclusions about the bias-variance tradeoff between Local Projections and Vector 
Autoregressions are robust to relaxing the equal-lag restriction. Specifically, 
we compare a fixed LP(4) estimator against a sweep of VAR specifications of 
increasing order, ranging from VAR(1) to VAR(p+H-1), thereby constructing a 
complexity-adjusted benchmark in the spirit of \textcite{ludwig2026local}. The 
central question is whether LP(4) retains its finite-sample MSE advantage over 
the equal-lag VAR(4) once the VAR is allowed to exploit its natural complexity 
advantage at longer horizons.

\textbf{DGP:} The baseline DGP is a stable bivariate zero-mean VAR(4) with 
recursive Cholesky identification. Recursive identification refers to the 
lower-triangular structure of the impact matrix $B$, obtained via Cholesky 
decomposition of the reduced-form error covariance matrix $\Sigma_u = BB'$. 
This imposes a contemporaneous zero restriction: the first variable does not 
respond on impact to shocks in the second variable, while the second variable 
may respond to both structural shocks simultaneously.

\begin{equation}
    y_t = A_1 y_{t-1} + A_2 y_{t-2} + A_3 y_{t-3} + A_4 y_{t-4} + u_t, 
    \quad u_t \sim (0, \Sigma_u), \quad y_t \in \mathbb{R}^2
\end{equation}

\begin{equation}
    \Sigma_u = BB', \qquad 
    B = \begin{pmatrix} b_{11} & 0 \\ b_{21} & b_{22} \end{pmatrix}, 
    \quad b_{11}, b_{22} > 0
\end{equation}

\begin{equation}
    \rho(\mathcal{A}) < 1, \qquad 
    \mathcal{A} = \begin{pmatrix} 
        A_1 & A_2 & A_3 & A_4 \\ 
        I_2 & 0   & 0   & 0   \\ 
        0   & I_2 & 0   & 0   \\ 
        0   & 0   & I_2 & 0   
    \end{pmatrix} \in \mathbb{R}^{8 \times 8}
\end{equation}

Persistence is calibrated via the spectral radius of the companion matrix 
across the empirically relevant range $\rho \in (0.5, 0.95)$. The lower 
bound reflects the observation that, for weakly persistent processes, impulse 
responses decay rapidly toward zero at longer horizons, rendering the 
bias-variance comparison between LP and VAR uninformative. The upper bound 
approaches the unit-root boundary while maintaining covariance stationarity, 
covering the high-persistence region where finite-sample differences between 
estimators are most pronounced.

\begin{equation}
    A_1 \leftarrow \frac{\rho}{\rho(\mathcal{A})} \cdot A_1, 
    \quad \rho \in (0.5,\, 0.95)
\end{equation}

Sample sizes are set to $T \in \{100, 200, 240, 500\}$, covering the range 
of quarterly macroeconomic samples typically encountered in applied work. 
The choice follows \textcite{herbst2024bias}, who document that the modal sample 
size in the structural VAR literature lies between 100 and 240 quarterly 
observations. The value $T = 500$ serves as a large-sample reference point 
to assess convergence toward the population equivalence result of 
\textcite{plagborg2021local}.

The structural estimand is $\theta_h = \Psi_h B e_1$, where $\Psi_h$ denotes 
the $h$-step ahead moving-average matrix of the reduced-form VAR, defined via 
$y_{t+h} = \sum_{j=0}^{\infty} \Psi_j u_{t+h-j}$ with $\Psi_0 = I_2$, $B$ 
is the Cholesky impact matrix, and $e_1 = (1,0)'$ is a selection vector. The 
primitive shock is normalized as a unit shock: we trace the dynamic response 
of the system to a one-standard-deviation increase in the first structural 
innovation $\varepsilon_{1t}$. Since $B$ is normalized such that 
$\mathrm{Var}(\varepsilon_t) = I_2$, no further rescaling is required.

\begin{equation}
    u_t = B\varepsilon_t, \qquad \varepsilon_t \overset{\text{iid}}{\sim} 
    \mathcal{N}(0, I_2)
\end{equation}

The innovation term is specified as Gaussian white noise. The reasoning behind 
Gaussianity is two-fold. First, our primary performance measures are bias, 
variance, and RMSE of the IRF estimates, which in population depend exclusively 
on the second moments of the underlying process, i.e.\ the VAR coefficients 
and the covariance matrix $\Sigma_u$. The distribution of the innovation term 
is therefore irrelevant for the point estimator comparison. Second, we follow 
\textcite{plagborg2021local}, who introduce Gaussianity explicitly as an 
expositional device that allows conditional expectations to be written in place 
of linear projections. As all results hold under covariance stationarity alone, 
the distributional assumption leaves the analysis qualitatively unchanged. 
Importantly, this choice is orthogonal to the misspecification parameters 
introduced in the extensions: varying the MA parameter $\theta$ in Extension~1 
or the nonlinearity parameter $\gamma$ in Extension~2 alters the structure of 
the DGP while leaving the innovation distribution fixed, ensuring that 
differences in estimator performance across designs are attributable solely to 
the respective misspecification channel.

\textbf{ESTIMAND:}
\begin{equation}
    \theta_h = \Psi_h B e_1, \qquad e_1 = (1, 0)', \qquad h = 1, \ldots, 20
\end{equation}

\textbf{METHODS:} We compare two classes of estimators. The first is a fixed 
LP(4) estimator, which serves as the reference point. At each horizon 
$h = 1, \ldots, H$, the LP(4) estimator is obtained from the OLS regression

\begin{equation}
    y_{1,t+h} = \mu_h + \beta_h x_t + \sum_{\ell=1}^{4} \delta_{h,\ell}' 
    y_{t-\ell} + \xi_{h,t}, \quad h = 1, \ldots, H
\end{equation}

where $x_t = e_1'(\hat{B}^{(4)})^{-1}\hat{u}_t^{(4)}$ is the estimated 
structural shock recovered from the first-stage VAR(4). The structural 
impulse response estimate at horizon $h$ is $\hat{\theta}_h^{\mathrm{LP}} 
= \hat{\beta}_h$.

The second class consists of a sweep of VAR estimators of increasing order, 
$\mathrm{VAR}(q)$ for $q = 1, \ldots, p + H - 1$, where $p = 4$ and $H = 20$. 
For each order $q$, the VAR is estimated equation-by-equation via OLS, the 
reduced-form covariance matrix $\hat{\Sigma}_u^{(q)}$ is Cholesky-decomposed 
to recover the structural impact matrix $\hat{B}^{(q)}$, and the $h$-step 
ahead structural IRF is computed as

\begin{equation}
    \hat{\theta}_h^{\mathrm{VAR}(q)} = \hat{\Psi}_h^{(q)} \hat{B}^{(q)} e_1
\end{equation}

where $\hat{\Psi}_h^{(q)}$ denotes the $h$-step ahead moving-average matrix 
of the estimated $\mathrm{VAR}(q)$. This sweep constitutes the 
complexity-adjusted benchmark motivated by \textcite{ludwig2026local}: by 
Theorem~1 of that paper, LP(4) at horizon $h$ is algebraically equivalent to 
a sequence of VAR predictions stepping through orders $4, 5, \ldots, 4 + h - 1$. 
The fair comparison is therefore not LP(4) against VAR(4) alone, but against 
the full range of VAR specifications up to order $p + H - 1$. Note that this 
equivalence holds exactly for the sequence of VARs of increasing order; 
comparing LP(4) to any single VAR$(q^*)$ is an approximation, with the 
complexity-adjusted single-order approximation given by $q^* = p + 
\frac{h-1}{2}$ \parencite{ludwig2026local}. The equal-lag VAR(4) is nested 
in the sweep as a special case and is retained as an additional reference 
point to connect to the conventional benchmark in the literature.

Identification of the structural shock is implemented as follows. For each 
$\mathrm{VAR}(q)$ in the sweep, the structural impact matrix $\hat{B}^{(q)}$ 
is obtained via Cholesky decomposition of the order-specific residual 
covariance matrix $\hat{\Sigma}_u^{(q)}$. For the LP(4), which does not 
estimate a complete system, identification is achieved through a first-stage 
VAR(4): the structural shock is constructed as $\hat{\varepsilon}_{1t} = 
e_1'(\hat{B}^{(4)})^{-1}\hat{u}_t^{(4)}$, consistent with the LP lag order 
$p = 4$. Since \textcite{plagborg2021local} establish that LP and VAR share 
the same implicit shock in population under recursive identification, this 
ensures that any divergence in estimated IRFs reflects differences in 
propagation rather than identification. No data-dependent lag selection is 
performed within the sweep; all VAR orders $q = 1, \ldots, p + H - 1$ are 
estimated on each simulated sample, ensuring that the comparison is across 
fixed specifications rather than across selection procedures.

\textbf{PERFORMANCE:} Performance is assessed along two dimensions, reported 
horizon-by-horizon for $h = 1, \ldots, 20$. For point estimation, we report 
bias, variance, MSE, and RMSE of the IRF estimates $\hat{\theta}_h$ relative 
to the true estimand $\theta_h$, computed across $B = 5{,}000$ Monte Carlo 
replications. The number of replications exceeds the $B = 1{,}000$ used by 
\textcite{li2024local} and \textcite{olea2025local}, ensuring tighter 
Monte Carlo standard errors at the cost of additional computation. Formally:

\begin{align}
    \widehat{\mathrm{Bias}}_h &= \frac{1}{B} \sum_{b=1}^{B} 
    \left(\hat{\theta}_h^{(b)} - \theta_h\right) \\
    \widehat{\mathrm{Var}}_h &= \frac{1}{B-1} \sum_{b=1}^{B} 
    \left(\hat{\theta}_h^{(b)} - \bar{\hat{\theta}}_h\right)^2 \\
    \widehat{\mathrm{MSE}}_h &= \frac{1}{B} \sum_{b=1}^{B} 
    \left(\hat{\theta}_h^{(b)} - \theta_h\right)^2 \\
    \widehat{\mathrm{RMSE}}_h &= \sqrt{\widehat{\mathrm{MSE}}_h}
\end{align}

Monte Carlo standard errors are reported alongside all point-estimator 
performance measures to quantify simulation uncertainty, with 
$\mathrm{MCSE}(\widehat{\mathrm{Bias}}_h) = \sqrt{\widehat{\mathrm{Var}}_h 
/ B}$.

For inference, we assess coverage of nominal $95\%$ confidence intervals. 
For the VAR$(q)$ sweep, confidence intervals are constructed via the delta 
method, propagating estimation uncertainty in the reduced-form coefficients 
$(\hat{A}_1, \ldots, \hat{A}_q)$ through the nonlinear mapping to structural 
IRFs $\hat{\theta}_h^{\mathrm{VAR}(q)} = \hat{\Psi}_h^{(q)} \hat{B}^{(q)} 
e_1$ \parencite{kilian2017structural}. For LP(4), confidence intervals are 
based on heteroskedasticity-robust standard errors, which suffice under the 
martingale difference assumption on the structural shock 
\parencite{plagborg2021local}. Coverage is defined as the fraction of 
replications in which the nominal $95\%$ interval contains the true estimand 
$\theta_h$:

\begin{equation}
    \widehat{\mathrm{Coverage}}_h = \frac{1}{B} \sum_{b=1}^{B} 
    \mathbf{1}\left[\theta_h \in \hat{CI}_h^{(b)}\right]
\end{equation}

\newpage
\subsection{Simulation Results}


% ----------------------------------- True IRF
\begin{figure}[htbp]
\centering
\caption{True Impulse Response Functions Across Persistence Regimes}
\label{fig:BL_TRUE_IRF}


\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{BASE_IRF_rho=0.5.png}
    \label{fig:BL_TRUE_IRF_LOW}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{BASE_IRF_rho=0.7.png}
    \label{fig:BL_TRUE_IRF_MID}
\end{subfigure}

\begin{subfigure}[t]{0.48\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{BASE_IRF_rho=0.95.png}
    \label{fig:BL_TRUE_IRF_HIGH}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each panel reports the true structural impulse response function, \(\theta_h = \Psi_h B e_1\), of variable 1 to a one-standard-deviation shock in the first structural innovation, traced over horizons \(h = 1, \ldots, 20\). Panels correspond to persistence regimes calibrated via the spectral radius of the companion matrix, \(\rho \in \{0.5, 0.7, 0.95\}\). Higher persistence yields slower decay of the response toward zero.
\end{minipage}

\end{figure}

\clearpage

% ----------------------------------- Point Estimates
\begin{figure}[htbp]
\centering
\caption{Point-Estimation Performance Across Persistence Regimes}
\label{fig:BL_PE}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{BASE_POINT_ESTIMATION_rho=0.5_T=240_B=5000.png}
    \label{fig:BL_LOW_PE}
\end{subfigure}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{BASE_POINT_ESTIMATION_rho=0.7_T=240_B=5000.png}
    \label{fig:BL_MID_PE}
\end{subfigure}

\begin{subfigure}[t]{0.6\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{BASE_POINT_ESTIMATION_rho=0.95_T=240_B=5000.png}
    \label{fig:BL_HIGH_PE}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each panel reports bias, variance, and RMSE of the estimated impulse responses \(\hat\theta_h\) relative to the true estimand \(\theta_h\), horizon-by-horizon for \(h = 1, \ldots, 20\), computed across \(B = 5{,}000\) Monte Carlo replications with \(T = 240\). The fixed LP(4) reference line is compared against the complexity-adjusted VAR\((q)\) sweep. Panels correspond to persistence regimes \(\rho \in \{0.5, 0.7, 0.95\}\). Shaded area in bias represents 95\% band from Monte Carlo standard errors.
\end{minipage}

\end{figure}

\clearpage

% ----------------------------------- Coverage
\begin{figure}[htbp]
\centering
\caption{Confidence-Interval Coverage Across Persistence Regimes}
\label{fig:BL_COV}

\begin{subfigure}[t]{0.7\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{BASE_COVERAGE_rho=0.5_T=240_B=5000.png}
    \label{fig:BL_LOW_CI}
\end{subfigure}

\begin{subfigure}[t]{0.7\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{BASE_COVERAGE_rho=0.7_T=240_B=5000.png}
    \label{fig:BL_MID_CI}
\end{subfigure}

\begin{subfigure}[t]{0.7\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{BASE_COVERAGE_rho=0.95_T=240_B=5000.png}
    \label{fig:BL_HIGH_CI}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each panel reports the empirical coverage of nominal \(95\%\) confidence intervals, defined as the fraction of replications in which the interval contains the true estimand \(\theta_h\), horizon-by-horizon for \(h = 1, \ldots, 20\) with \(T = 240\) and \(B = 5{,}000\). VAR\((q)\) intervals are constructed via the delta method; LP(4) intervals use heteroskedasticity-robust standard errors. Panels correspond to persistence regimes \(\rho \in \{0.5, 0.7, 0.95\}\).
\end{minipage}

\end{figure}

\clearpage

% ----------------------------------- Sample RMSE by Horizon
\begin{figure}[htbp]
\centering
\caption{Sample RMSE by Horizon}
\label{fig:BL_SAMPLE_RMSE}

\begin{subfigure}[t]{0.5\textwidth}
    \centering
    \caption{\(\rho = 0.5\)}
    \includegraphics[width=\textwidth]{BASE_RMSE_BY_HORIZON_rho=0.5.png}
    \label{fig:BL_SAMPLE_RMSE_rho05}
\end{subfigure}

\begin{subfigure}[t]{0.5\textwidth}
    \centering
    \caption{\(\rho = 0.7\)}
    \includegraphics[width=\textwidth]{BASE_RMSE_BY_HORIZON_rho=0.7.png}
    \label{fig:BL_SAMPLE_RMSE_rho07}
\end{subfigure}

\begin{subfigure}[t]{0.5\textwidth}
    \centering
    \caption{\(\rho = 0.95\)}
    \includegraphics[width=\textwidth]{BASE_RMSE_BY_HORIZON_rho=0.95.png}
    \label{fig:BL_SAMPLE_RMSE_rho095}
\end{subfigure}

\begin{minipage}{0.9\textwidth}
\small
\textit{Note.} Each panel reports RMSE by horizon for the base DGP with varying $T$. Sub-graphs correspond to persistence values $\rho \in \{0.5,0.7,0.95\}$.
\end{minipage}

\end{figure}

\FloatBarrier

\subsection{Discussion of Results}

\FloatBarrier